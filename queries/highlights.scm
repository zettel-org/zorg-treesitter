; Zorg syntax highlighting for the public Epic 2 grammar nodes.

(file_header_open
  (title_text) @markup.heading)

(header_line
  (title_text) @markup.heading)

(zettel_opening
  (title_text) @markup.heading)

(id) @constant

(local_id) @constant.special

(absolute_link) @markup.link
(percent_fence) @punctuation.delimiter

(child_link) @markup.link

(sibling_link) @markup.link

(tag) @tag

(type_tag) @tag.builtin

(property_key) @property

(property_value) @string

(todo_marker) @keyword

(list_marker) @punctuation.special

(code_fence_delimiter) @punctuation.special

(info_string) @label
